eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8765
spring:
  application:
    name: service-feign

#默认feign的hystrix不打开的 D版之后
feign:
  hystrix:
    enabled: true